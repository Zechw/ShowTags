Basic script to parse Git tags in the format `<name>/<semantic-version>-<env>.<attempt>`

Use by pulling down the repo; running `pip install -e .` then use `showtags` from the command line while in another repo.
